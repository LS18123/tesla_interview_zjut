本文档负责说明在代码运行过程中需要用到的库

另请注意在真实代码运行中更换自己的API key

本项目中的代码为了方便演示在运行时采用的是绝对索引的形式，建议根据自身文档的储存情况更换地址或换为相对索引


# 基础框架与 RAG 编排
langchain>=0.1.0
langchain-community
langchain-openai  # 如果你使用的是 OpenAI 或兼容接口

# PDF 解析与处理
pdfplumber>=0.10.0 
markdown>=3.5

# 向量数据库与 Embedding
chromadb>=0.4.0 
sentence-transformers>=2.2.0 [cite: 39]
pydantic>=2.0.0

# 环境管理与工具
python-dotenv>=1.0.0
tqdm

# 交互界面
gradio>=4.0.0
